#include <stdio.h>
#include <math.h>
using namespace std;
bool koniec=false;
int n;
int teraz=0;
int proby=0;
long double maximum=1.8E+300;
bool nigdy=false;
int main()
{
    scanf("%i",&n);
    long double pola[n-1][3];
    for(int i=0;i<(n-1);i++){
        for(int j=0;j<3;j++){
            if(j!=2){
                scanf("%i",&pola[i][j]);
            }else{
                pola[i][j]=1;
            }
        }
    }

    return 0;
}
